# ============================================================================
# PanziPool 环境变量配置模板
# ----------------------------------------------------------------------------
# 使用方法：
#   1. 复制此文件为 .env：cp .env.example .env
#   2. 修改下方各项为实际值（特别是 DB_PASSWORD 必须修改）
#   3. docker-compose up -d 启动服务
#
# 注意：.env 文件包含敏感信息，请勿提交到版本控制系统
# ============================================================================

# ----------------------------------------------------------------------------
# PostgreSQL 数据库配置
# ----------------------------------------------------------------------------

# 数据库名称
DB_NAME=panzipool

# 数据库用户名
DB_USERNAME=postgres

# 数据库密码（必须修改为强密码！请使用随机生成的长密码）
DB_PASSWORD=请修改为强密码_例如_Pz2026SecureP@ssw0rd!

# ----------------------------------------------------------------------------
# 后端 API 配置
# ----------------------------------------------------------------------------

# CORS 允许的源（逗号分隔，生产环境仅允许主站域名）
CORS_ORIGINS=https://www.panzipool.com

# ----------------------------------------------------------------------------
# 前端配置
# ----------------------------------------------------------------------------

# 客户端浏览器访问的 API 基础地址（浏览器通过此地址访问后端 API）
# 生产环境应设置为公网域名（nginx 会将 /api/v1/ 请求反代到后端容器）
PUBLIC_API_BASE=https://www.panzipool.com
