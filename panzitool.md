# 工具聚合类网站（面向中文用户的在线小工具集合）

**Status:** COMPLETE

**Timeline:** 1个月内上线MVP
**Budget:** $2,000

## Project Overview

**Problem Statement:** 面向中文用户的在线工具聚合站，核心满足开发者工具需求（约70%），并提供少量常用图片工具（约30%）。用户无需安装即可在线完成格式化/编码解码/时间转换/正则测试等以及图片压缩/转格式等操作，并通过分类与搜索快速找到工具。
**Ideal Customer Profile:** 通用开发者（中文用户为主），需要快速进行联调/数据处理/格式转换/编码解码/时间处理等日常工作；同时偶尔需要图片压缩与格式转换等轻量图片处理。
**App Audience:** Public-facing app（公开网站，任何人可访问）
**Project Type:** Greenfield
**Existing App Details:** 无（新项目）
**Integrations:** 广告系统 + 统计分析（选择：百度统计）
**Tech Stack Preferences:** 前端采用Vue（接受Nuxt3以支持SSR/SEO）；后端采用Java（Spring Boot 3.x + JDK 17）；ORM选择Spring Data JPA（MVP优先）+ Flyway做数据库迁移；数据库PostgreSQL；支持Docker容器化部署。
**Deployment Preferences:** 腾讯云；Docker容器化部署（建议docker-compose：nginx反代 + nuxt前端 + Java API + postgres；云服务器/具体规格TBD）

## Features

### 正则表达式测试工具

输入正则与测试文本，展示匹配结果、分组信息，支持flags（g/i/m/s/u等）。

- 正则测试UI: 正则输入、flags选择、测试文本输入、结果展示区。
- 匹配与高亮逻辑: 执行RegExp匹配，展示match列表与分组；在文本中高亮命中片段。
- 异常处理: 非法正则时提示错误信息。
- 工具页SEO与说明: 路由、说明、示例。

### 时间戳转换工具

支持秒/毫秒时间戳与日期时间互转，提供当前时间戳一键获取与复制。

- 时间戳UI: 时间戳输入、日期输入、转换按钮、当前时间戳、复制。
- 转换逻辑: 实现秒/毫秒识别与Date转换，校验非法输入。
- 工具页SEO与说明: 路由、说明与示例。

### 图片压缩工具

在浏览器本地压缩图片（jpg/png/webp），可设置质量/最大宽高，导出下载。

- 压缩UI: 上传/拖拽、参数（质量/宽高）、预览、前后大小对比、下载。
- 本地压缩逻辑: Canvas重编码或使用库；确保不上传服务器（默认）。
- 多文件支持（基础）: 支持一次选择多张并逐个导出（MVP可简单列表）。
- 工具页SEO与说明: 路由、隐私说明、示例。

### 站点基础框架（首页/分类/搜索/SEO/隐私）

实现工具聚合站的基础信息架构：首页、分类页、全站搜索、基础SEO能力，以及关于/隐私政策页面。

- 信息架构与路由: 首页、分类页、工具详情页路由规划；面包屑导航与站内链接结构。
- 首页实现: 分类入口、搜索框、热门/最新工具区块、推荐工具卡片布局。
- 分类页实现: 开发者工具/图片工具分类列表页，支持排序（热门/最新）。
- 全站搜索（工具名/关键词）: 前端本地搜索优先；后续可扩展为后端搜索。
- 基础SEO: 每个工具独立URL；title/description；站点地图sitemap.xml；robots.txt；结构化数据（可选）。
- 关于/隐私政策页面: 提供站点介绍、联系/反馈方式（可TBD）、隐私政策（强调图片本地处理/不上传的声明）。

### URL 编码/解码工具

对字符串进行 URL Encode/Decode，支持一键复制结果与常见异常提示。

- URL工具UI: 输入区、输出区、Encode/Decode切换或按钮、复制/清空、示例。
- 编码解码逻辑: 实现encodeURIComponent/decodeURIComponent并处理异常输入。
- 工具页SEO与说明: 路由与元信息、说明/示例。

### JWT 解析工具

输入JWT字符串，解析并展示Header与Payload（JSON美化），提示过期时间等基础字段。

- JWT解析UI: 输入区、解析按钮、Header/Payload输出区、复制。
- 解析逻辑: 按点分段，base64url解码并JSON解析；处理错误提示。
- 基础字段提示: 若包含exp/iat等字段，转换为可读时间。
- 工具页SEO与说明: 路由、说明、示例与安全提示（不上传/不验签）。

### 哈希计算工具（MD5/SHA）

对文本计算MD5、SHA1、SHA256等哈希值并支持复制。

- 哈希工具UI: 输入区、算法选择、输出区、复制。
- 哈希计算实现: 使用Web Crypto API实现SHA系列；MD5可用轻量库（后续可替换）。
- 工具页SEO与说明: 路由、说明、示例。

### Base64 编码/解码工具（文本）

支持文本 Base64 编码与解码，处理UTF-8字符；提供复制与示例。

- Base64工具UI: 输入区、输出区、编码/解码按钮、复制/清空。
- Base64 UTF-8处理: 实现对中文等UTF-8字符的正确编码解码（TextEncoder/TextDecoder）。
- 工具页SEO与说明: 路由、元信息、说明与示例。

### 图片裁剪/改尺寸工具

支持裁剪、旋转（可选）、按固定尺寸或自定义尺寸导出。

- 裁剪UI: 上传、裁剪框、比例选择、尺寸设置、预览、下载。
- 裁剪与导出逻辑: Canvas裁剪与重采样，导出png/jpg。
- 工具页SEO与说明: 路由、说明、示例。

### 图片格式转换工具

在本地将图片在png/jpg/webp之间转换，提供预览与下载。

- 转换UI: 上传、选择目标格式、预览、下载。
- 转换逻辑: Canvas导出不同mime，处理透明背景提示（png->jpg）。
- 工具页SEO与说明: 路由、说明、示例。

### 广告位集成（预留+可配置）

在站点关键位置预留广告位，并支持通过配置注入广告代码片段（如AdSense/联盟广告，具体平台待定）。

- 广告位规划: 定义首页/分类页/工具页的广告位位置与尺寸（如顶部横幅、侧边栏、文末）。
- 广告组件封装: 封装可配置的广告组件，支持开关、不同广告位key。
- 注入脚本与加载策略: 支持注入第三方广告脚本；注意异步加载、避免阻塞首屏。
- 合规与降级策略: 未配置或加载失败时不影响页面布局；提供隐私/免责声明链接。

### 匿名使用统计与点赞系统（后端+数据库）

在不登录的前提下，记录匿名访问次数与工具使用次数；为每个工具提供点赞按钮并记录点赞次数（防刷策略基础版）。

- 数据模型设计: 设计数据库表：tools（工具元数据）、tool_events（按日/按事件聚合可选）、tool_likes（计数或日志），以及站点访客统计（按日PV/UV估算）。
- 匿名统计埋点上报API: 后端提供事件上报接口：page_view、tool_use、copy、download等；前端在关键操作触发上报。
- 点赞API与防刷（基础）: 点赞/取消点赞接口；基于cookie/localStorage生成匿名id；限制频率（如同一匿名id对同一工具仅一次或冷却时间）。
- 工具页展示计数: 在工具页展示使用次数/点赞次数；在列表页可展示热门排序依据。
- 管理配置（可选轻量）: 是否需要简单管理入口配置工具信息/是否展示计数（可先写死配置文件，后续再做后台）。

### JSON 格式化/压缩/校验工具

提供 JSON 输入的格式化、美化、高亮展示、压缩输出与语法校验；错误时提示位置并给出友好提示。

- JSON编辑器UI: 输入区/输出区、格式化/压缩按钮、复制按钮、清空按钮、示例填充。
- JSON解析与错误提示: 使用JSON.parse校验，捕获异常并提示行列/片段（尽量友好）。
- 格式化与压缩逻辑: 实现JSON.stringify格式化（可配置缩进）与压缩输出。
- 工具页SEO与说明: 独立路由、title/description、使用说明与示例、FAQ区块。

### 统计分析接入（PV/工具使用）

接入站点统计分析，跟踪页面访问与工具使用事件，支持后续迭代决策。

- 选择统计平台（TBD）: 确定使用GA/Umami/百度统计等；记录选择与配置方式。
- 全站埋点接入: 接入统计脚本并配置单页应用路由切换的PV统计。
- 关键事件埋点: 工具页“格式化/转换/下载/复制”等关键按钮触发事件上报。
- 隐私合规提示: 在隐私政策中说明统计用途；必要时提供关闭/匿名策略（可选）。

## Milestones

### Milestone 1: 里程碑 1：项目骨架 + 后端/数据库基础 + 统计/点赞基础

搭建前后端与数据库基础、Docker化；实现站点框架（首页/分类/搜索/SEO/隐私）；完成匿名统计上报API与点赞（不可取消，一设备一次）基础能力。

**Payment:** $800

- 站点基础框架（首页/分类/搜索/SEO/隐私）
- 匿名使用统计与点赞系统（后端+数据库）

### Milestone 2: 里程碑 2：核心开发者工具（7个）+ 计数/点赞展示

交付7个开发者工具并接入匿名统计/点赞展示：JSON、URL、Base64、时间戳、正则、JWT、哈希。

**Payment:** $700

- 正则表达式测试工具
- 时间戳转换工具
- URL 编码/解码工具
- JWT 解析工具
- 哈希计算工具（MD5/SHA）
- Base64 编码/解码工具（文本）
- JSON 格式化/压缩/校验工具

### Milestone 3: 里程碑 3：图片工具（3个）+ 广告与第三方统计上线 + 部署交付

交付3个图片工具（压缩/格式转换/裁剪改尺寸），接入广告位与第三方统计平台，完成Docker部署文档与上线支持。

**Payment:** $500

- 图片压缩工具
- 图片裁剪/改尺寸工具
- 图片格式转换工具
- 广告位集成（预留+可配置）
- 统计分析接入（PV/工具使用）

## Skills Needed

### Full-Stack Developer

负责Vue前端、后端API、数据库设计、统计/点赞、广告与部署（Docker）全栈交付。

**Skills:** Vue, TypeScript, Node.js, REST API, PostgreSQL, Docker, SEO, Web Analytics, HTML/CSS

## Open Questions

- 匿名访问/工具使用次数/点赞：统计口径与防刷策略如何定义？（初版建议：按天聚合PV/使用/复制/下载；点赞=同一anon_id一次不可取消）
  Context: 补充说明：第三方统计平台已确定为百度统计，hm.js?id=06c8d960aee8a68f0a9a229ff4a18ceb。自建后端统计仍用于站内展示“使用次数/点赞次数”。建议统计口径：工具页PV与工具使用事件分别记录；事件按天聚合；匿名ID使用localStorage生成UUID；点赞表对(tool_id, anon_id)唯一约束防重复。
- 统计/点赞报表后台需求已取消
  Context: 用户确认暂时去掉报表后台页需求；当前仅保留前台展示计数（使用次数、点赞次数）与必要的事件上报。若后续需要后台报表再重新评估实现与访问控制方式。
- 域名主站定为 www.panzipool.com：是否需要将 panzipool.com 301 跳转到 www，以及HTTPS证书与DNS配置如何安排？
  Context: 用户选择主域名使用 www.panzipool.com。需要明确DNS解析、是否强制HTTPS、以及Nginx/反代层面将裸域 panzipool.com 301 到 www 以避免SEO与统计数据分裂。

---

